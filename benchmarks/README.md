# Benchmarks

Benchmark baselines for ORM hot paths (query compilation, row decoding) will be added starting in Phase 9.

Results are saved under `benchmarks/results/` (gitignored until baselines are established in later phases).
